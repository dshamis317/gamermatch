<div class="container">
  <h2 class="users_index">Current Members:</h2>
  <ul>
    <% @users.each do |user| %>
    <li class="users"><a href="/users/<%= user.id %>/games"><%= user.username %></a></li>
    <% end %>
  </ul>
</div>
