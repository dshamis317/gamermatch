<div class="container">
  <h2>Who's playing <%= @game.title %>?</h2>
  <ul>
    <% @users.each do |user| %>
      <li>
        <a href="/users/<%= user.id %>/games"><%= user.username %></a>
      </li>
    <% end %>
  </ul>
</div>
