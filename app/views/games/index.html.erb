<%= render "nav" %>

<div class="container">
  <% @games.each do |game| %>
  <div class="index_games">
    <h3><a href="/users/<%= @user.id %>/games/<%= game.id %>"><%= game.title %></a></h3>
    <img src="<%= game.image_url %>">
  </div>
  <% end %>
</div>
