<h2 id="table_header">Current Games Being Played</h2>
<hr>
<table class="games_index">
  <% @games.each do |game| %>
  <tr>
    <td><img src="<%= game.image_url %>"></td>
    <td><a href="/games/search/<%= game.gb_id %>"><%= game.title %></a></td>
    <td><%= game.platform %></td>
  </tr>
  <% end %>
</table>
<hr>
